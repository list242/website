
# Website - Movie Collection API

REST API сервис для управления коллекцией фильмов на FastAPI с веб-интерфейсом.

## Описание

Полнофункциональное веб-приложение для управления фильмами в базе данных PostgreSQL с использованием:
- **Backend**: FastAPI (асинхронный REST API)
- **Frontend**: HTML/CSS/JavaScript интерфейс
- **Database**: PostgreSQL с SQLAlchemy ORM
- **Containerization**: Docker & Docker Compose для развёртывания

## Функционал

- ✅ Просмотр списка всех фильмов
- ✅ Добавление новых фильмов в БД
- ✅ Редактирование информации о фильмах
- ✅ Удаление фильмов из коллекции
- ✅ REST API с интерактивной документацией (Swagger/ReDoc)
- ✅ Hot reload при разработке

## Структура проекта

```
.
├── main.py              # FastAPI приложение с маршрутами
├── models.py            # SQLAlchemy модели (таблица Movie)
├── schemas.py           # Pydantic схемы валидации данных
├── database.py          # Конфигурация PostgreSQL подключения
├── requirements.txt     # Зависимости Python
├── Dockerfile          # Конфигурация Docker контейнера
├── docker-compose.yml  # Docker Compose конфигурация
├── templates/
│   └── index.html      # Веб-интерфейс приложения
└── static/
    ├── style.css       # Стили оформления
    └── script.js       # JavaScript для взаимодействия с API
```

## Требования

- Python 3.9+
- Docker & Docker Compose
- PostgreSQL 15+ (через Docker)

## Быстрый старт

```
# Перейдите в папку проекта
cd website

# Запустите контейнеры
docker-compose up --build
```

Приложение будет доступно на:
- 🌐 Веб-интерфейс: **http://localhost:8001/**
- 📚 Swagger API: **http://localhost:8001/docs**
- 📖 ReDoc API: **http://localhost:8001/redoc**

docker-compose down
```
```
